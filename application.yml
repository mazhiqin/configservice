spring:
  profiles:
    active:
      - dev

---

spring:
  port: 8082
  profiles: dev
  application:
    name: configservice-dev
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/


---

spring:
  profiles: test
  application:
    name: configservice-test
